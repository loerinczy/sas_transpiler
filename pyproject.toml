[project]
name = "sas-transpiler"
version = "0.1.0"
description = "SAS to PySpark transpiler"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "src"]
addopts = "--import-mode=importlib"
